import { View, Text } from 'react-native'
import React from 'react'

const catalouge = ({ customer }: { customer?: { name: string } }) => {
    return (
        <View>
            <Text>catalouge</Text>
        </View>
    )
}

export default catalouge